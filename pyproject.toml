[project]
name = "rag-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "muhammed sahal", email = "muhammedsahalap717@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "chromadb>=1.5.9",
    "google-genai>=2.19.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
rag-cli = "rag_cli:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
